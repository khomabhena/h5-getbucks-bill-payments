/**
 * Bill payments catalog — backed by VAS BFF (replaces AppleTree sandbox).
 */
export {
  vasCatalogService as appleTreeService,
  vasCatalogService as default,
  SERVICES,
} from './vas/vasCatalogService.js';
